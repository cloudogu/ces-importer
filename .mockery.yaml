with-expecter: True
all: True

packages:
  github.com/cloudogu/ces-importer:
    config:
      recursive: True
      exclude: ["vendor", "cron", "main"]
      inpackage: true
      include-auto-generated: false
      filename: "mock_{{.InterfaceName}}_test.go"
      dir: "."